# Быстрая инструкция / Quick Guide

## 🚀 Запуск приложения

### На компьютере (Windows/Mac/Linux)

1. **Установите Python 3.8+**
   - Windows: https://www.python.org/downloads/
   - Mac: `brew install python3`
   - Linux: `sudo apt install python3 python3-pip`

2. **Установите зависимости:**
   ```bash
   pip install kivy sympy
   ```

3. **Запустите калькулятор:**
   ```bash
   python main.py
   ```

### На Android

1. **Скачайте APK** из релизов GitHub или соберите сами
2. **Установите APK** на устройство
3. **Разрешите** установку из неизвестных источников (если требуется)
4. **Запустите** приложение "Калькулятор Уравнений"

## 📱 Как пользоваться

### Шаг 1: Введите уравнение
В поле "Уравнение:" введите ваше уравнение, например:
- `x**2 - 4 = 0`
- `log(x) + 2 = 5`
- `exp(x) = 7`

### Шаг 2: Нажмите "Решить"
Приложение автоматически:
- Разберёт уравнение
- Найдёт все решения
- Покажет пошаговое решение
- Проверит корректность

### Шаг 3: Просмотрите решение
Прокрутите вниз, чтобы увидеть:
- Исходное уравнение
- Промежуточные шаги
- Окончательные решения
- Численные значения
- Проверку решений

## 📝 Синтаксис ввода

### Операции
- Сложение: `+` → `x + 5`
- Вычитание: `-` → `x - 3`
- Умножение: `*` → `2*x` или `2x`
- Деление: `/` → `x/2`
- Степень: `**` → `x**2` (x²)

### Функции
- Логарифм (натуральный): `log(x)`
- Логарифм по основанию: `log(x, 10)` (log₁₀x)
- Экспонента: `exp(x)` (eˣ)
- Корень: `sqrt(x)` или `x**0.5`
- Синус: `sin(x)`
- Косинус: `cos(x)`
- Тангенс: `tan(x)`

### Константы
- π: `pi` → 3.14159...
- e: `E` → 2.71828...

## 💡 Примеры

### Простые
```
x + 5 = 10           → x = 5
2*x = 8              → x = 4
x/3 = 2              → x = 6
```

### Квадратные
```
x**2 = 9             → x = -3, x = 3
x**2 + 2*x + 1 = 0   → x = -1
x**2 - 5*x + 6 = 0   → x = 2, x = 3
```

### Логарифмические
```
log(x) = 2           → x = e² ≈ 7.389
log(x, 10) = 2       → x = 100
log(x, 2) = 3        → x = 8
```

### Экспоненциальные
```
exp(x) = 20          → x = ln(20) ≈ 2.996
2*exp(x) = 10        → x = ln(5) ≈ 1.609
```

### Тригонометрические
```
sin(x) = 0.5         → x = π/6, x = 5π/6
cos(x) = 0           → x = π/2, x = 3π/2
tan(x) = 1           → x = π/4
```

## 🔧 Сборка APK (кратко)

### Требования
- Linux (Ubuntu 20.04+)
- Python 3.8+
- Java JDK 17
- 20 GB свободного места

### Команды
```bash
# Установка Buildozer
pip install buildozer

# Сборка (первый раз 30-90 минут)
buildozer android debug

# APK будет в папке bin/
```

**Подробно:** См. файл `ANDROID_BUILD.md`

## ❓ Частые вопросы

### Приложение не запускается?
```bash
pip install --upgrade kivy sympy
python main.py
```

### Ошибка "module not found"?
```bash
pip install -r requirements.txt
```

### Не решает уравнение?
- Проверьте синтаксис
- Используйте `*` для умножения
- Используйте `**` для степени
- Переменная должна быть `x`

### Как решить систему уравнений?
Пока не поддерживается. Решайте по одному уравнению.

### Можно без GUI?
Да, используйте `demo.py`:
```bash
python demo.py
```

## 📞 Помощь

- **Документация:** README.md
- **Примеры:** EXAMPLES.md
- **Android:** ANDROID_BUILD.md
- **GitHub:** https://github.com/Slava47/calclog

## ⚡ Горячие клавиши

- **Enter** в поле ввода → Решить
- **Пример** → Загрузить случайный пример
- **Очистить** → Очистить всё

---

**Версия:** 1.0  
**Лицензия:** MIT  
**Автор:** Slava47
